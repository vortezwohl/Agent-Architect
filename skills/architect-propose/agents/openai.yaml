interface:
  display_name: "Architect Propose"
  short_description: "Manual-only: seal an approved plan package"
  default_prompt: "Run only when the user explicitly invokes $architect-propose. Turn an already approved D-xxx design bundle into a sealed package under .architect/. Do not auto-invoke $architect-build."
