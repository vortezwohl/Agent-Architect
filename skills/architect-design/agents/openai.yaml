interface:
  display_name: "Architect Design"
  short_description: "Manual-only: approve D-xxx design units"
  default_prompt: "Run only when the user explicitly invokes $architect-design. Read the required references, resolve compatibility intent, and produce approved D-xxx design units. Do not auto-invoke $architect-propose or $architect-build."
