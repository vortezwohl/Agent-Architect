interface:
  display_name: "Architect Build"
  short_description: "Build one validated architecture plan package at a time"
  default_prompt: "Use $architect-build to preflight the approved plan package, implement one bounded task, run its verification, and log the result."
