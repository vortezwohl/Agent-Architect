interface:
  display_name: "Architect Build"
  short_description: "Manual-only: execute one sealed task safely"
  default_prompt: "Run only when the user explicitly invokes $architect-build. Execute one sealed T-xxx task with scope checks and checkpoint recovery. Do not auto-invoke $architect-design or $architect-propose."
